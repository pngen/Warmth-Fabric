add_executable(example_lifecycle example_lifecycle.cpp)
target_link_libraries(example_lifecycle PRIVATE warmth)
target_compile_features(example_lifecycle PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_lifecycle PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_lifecycle PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_lifecycle PRIVATE -Wall -Wextra -Werror)
endif()

add_executable(example_readiness example_readiness.cpp)
target_link_libraries(example_readiness PRIVATE warmth)
target_compile_features(example_readiness PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_readiness PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_readiness PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_readiness PRIVATE -Wall -Wextra -Werror)
endif()

add_executable(example_decay_invalidation example_decay_invalidation.cpp)
target_link_libraries(example_decay_invalidation PRIVATE warmth)
target_compile_features(example_decay_invalidation PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_decay_invalidation PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_decay_invalidation PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_decay_invalidation PRIVATE -Wall -Wextra -Werror)
endif()

add_executable(example_budget_schedule example_budget_schedule.cpp)
target_link_libraries(example_budget_schedule PRIVATE warmth)
target_compile_features(example_budget_schedule PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_budget_schedule PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_budget_schedule PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_budget_schedule PRIVATE -Wall -Wextra -Werror)
endif()

add_executable(example_persistence example_persistence.cpp)
target_link_libraries(example_persistence PRIVATE warmth)
target_compile_features(example_persistence PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_persistence PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_persistence PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_persistence PRIVATE -Wall -Wextra -Werror)
endif()

add_executable(example_stale_authority example_stale_authority.cpp)
target_link_libraries(example_stale_authority PRIVATE warmth)
target_compile_features(example_stale_authority PRIVATE cxx_std_20)
if(WIN32)
    target_link_libraries(example_stale_authority PRIVATE ws2_32)
endif()
if(MSVC)
    target_compile_options(example_stale_authority PRIVATE /W4 /WX /permissive- /Zc:__cplusplus /EHsc)
else()
    target_compile_options(example_stale_authority PRIVATE -Wall -Wextra -Werror)
endif()

